name: lpce
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - biopython
  - tqdm
  - joblib
  - yagmail
  - rsync
  - pathlib
  - pip
  - datamol
  - pyarrow
  - ipykernel
  - fastparquet
  - mols2grid
  - pandarallel
  - numpy
  - pip:
    - python-dotenv
    - tqdm
    - pytest
    - chembl_structure_pipeline
